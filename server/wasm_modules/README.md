# WebAssembly modules directory
#
# Place compiled .wasm files here for use by code_exec_tool.
# These files are excluded from version control via .gitignore.
#
# Example build:
#   cd wasm_modules && cargo build --target wasm32-wasi --release
