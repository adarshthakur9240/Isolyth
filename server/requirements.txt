# ── Core MCP & HTTP server ─────────────────────────────────────────────────────
# Pinned to exact locally-verified working versions.
# Run `pip show <package>` in your venv to confirm before upgrading.
mcp==1.28.1
fastapi==0.140.7
uvicorn[standard]==0.51.0
starlette==1.3.1
pydantic==2.13.4

# ── Database ───────────────────────────────────────────────────────────────────
asyncpg==0.31.0

# ── Authentication ────────────────────────────────────────────────────────────
python-jose[cryptography]==3.5.0
cryptography==49.0.0

# ── Caching & rate limiting ───────────────────────────────────────────────────
redis==8.0.1

# ── WASM sandboxing ───────────────────────────────────────────────────────────
wasmtime==47.0.1

# ── Observability ─────────────────────────────────────────────────────────────
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-instrumentation-fastapi==0.65b0
prometheus-client==0.26.0

# ── HTTP client ───────────────────────────────────────────────────────────────
httpx==0.28.1

# ── Async runtime ─────────────────────────────────────────────────────────────
anyio==4.14.2

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov

# ── Load testing ─────────────────────────────────────────────────────────────
locust==2.46.2

# ── Linting ───────────────────────────────────────────────────────────────────
ruff
